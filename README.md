# WinterPTer (PySide6)

这是一只WinterPT。

## 快捷键

ctrl + alt + q 退出
ctrl + alt + p 鼠标可穿透
ctrl + alt + l 隐藏

## 运行
```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
pip install -e .
python -m desktop_pet